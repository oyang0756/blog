<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>评论管理 - 博客</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Noto+Serif+SC:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{> header}}

    <main class="main-content">
        <div class="admin-page">
            <h1>评论管理</h1>
            <a href="/admin" class="btn">返回后台</a>

            <table class="admin-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>文章</th>
                        <th>作者</th>
                        <th>内容</th>
                        <th>时间</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each comments}}
                    <tr>
                        <td>{{id}}</td>
                        <td><a href="/post/{{post_slug}}">{{post_title}}</a></td>
                        <td>{{author_username}}</td>
                        <td class="comment-content">{{content}}</td>
                        <td>{{formatDate created_at}}</td>
                        <td>
                            <form method="POST" action="/admin/comments/{{id}}/delete" class="delete-form" onsubmit="return confirm('确定删除该评论？')">
                                <button type="submit" class="btn btn-danger">删除</button>
                            </form>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </main>

    {{> footer}}
</body>
</html>
